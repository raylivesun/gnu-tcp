;; Name Project: "GNU Emacs Business Good View AI and Pamps"
;; Name Program: "GNU Emacs Shark AI EPamps"
;; Name Extension: "Lisp"
;; Name Data 512
;; Name Unit 512

(provide 'pamps)
(require 'pamps)

(load("/home/admin/app/locust/gnu-tcp/matrix/gnu/bin/path-current/path-dialog/pg612.lisp"))

(defmacro ai-epamps (ai &optional pamps
			:quick dns)
  (declare (ai &optional pamps
	       :quick dns)
	   (let ((go dns)
		 (t (function dns)
		    (:dns --static pamps
			  :good views)))
	     (lambda ((ai &optional pamps)
		      (t (function ai)
			 (:ai --static pamps
			      :good view))
		      (t (function pamps)
			 (:pamps --static tools
				 :good view)))
	       (interactive :dns args))))
  'plans
  :quick dns)
		
		 
